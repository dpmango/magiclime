import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles({
  root: {
    position: 'relative',
    top: '-95px',
  },
  content: {
    marginTop: 48,
  },
  tags: {
    marginTop: 32,
    maxWidth: 720,
  },
  main: {
    marginTop: 36,
  },
});

export default useStyles;
