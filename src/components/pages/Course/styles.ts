import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles({
  root: {
    position: 'relative',
  },
  container: {
    maxWidth: '1208px',
    paddingLeft: 40,
    paddingRight: 40,
    marginLeft: 'auto',
    marginRight: 'auto',
  },
  tabs: {
    marginTop: 36,
  },
});

export default useStyles;
