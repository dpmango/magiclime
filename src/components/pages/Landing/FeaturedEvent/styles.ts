import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles({
  root: {
    position: 'relative',
    margin: '80px 0',
  },
  banner: {
    width: '565px',
    height: '345px',
    objectFit: 'cover',
    borderRadius: '7.5px',
  },
});

export default useStyles;
